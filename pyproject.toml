[project]
name = "management-github-deploy-roles"
version = "0.1.0"
description = "GitHub Actions OIDC IAM roles management"
requires-python = ">=3.11"
dependencies = [
    "cookiecutter==2.6.0",
    "pyyaml==6.0.3",
]

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
    "ruff>=0.9.0",
]
